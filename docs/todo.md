# 待办清单

最后更新：2026-07-16

本文件记录待开发、待完善、待部署和待验证事项。内容依据 `docs/ai-project-context.md`、阶段状态文档和当前本地工作区整理。

优先级说明：

- `P0`：阻塞核心的路线规划、保存、分享、现场体验闭环。
- `P1`：核心闭环可用后，应优先补齐的重要质量或可靠性工作。
- `P2`：体验打磨、视觉还原和更长期的产品扩展。

## 尚未提交或部署

以下事项看起来已在本地实现，但尚未完整上线。

- `P0` 提交并推送当前本地工作区。
  - 本地改动包含规划流程、高德路线代理、打卡图 repository/storage 迁移、站点角色规则、分享删除、收藏路线、文档、测试和 UI 样式。
  - 在提交并推送前，GitHub Pages 不会部署这些变更。
- `P0` 部署更新后的 `amap-proxy` Edge Function。
  - 本地函数已支持 POI 返回上限 15，并支持步行、公交、驾车路线调用；打车复用驾车耗时。
  - 已部署版本可能不包含这些最新改动。
  - 确认 Supabase 登录/API 状态后，可执行：
    `npx supabase functions deploy amap-proxy --project-ref wedwvcmdbrnbzjwlllgl --use-api`。
- `P0` 应用待执行的 Supabase 数据库/Storage 迁移。
  - 本地迁移文件：`supabase/migrations/20260715000200_route_checkin_photos.sql`。
  - 该迁移创建 `route_checkin_photos`，并为私有 `route-media` Storage 配置策略。
  - 之前执行 `supabase db push --linked --yes` 时，因为环境没有数据库密码，卡在远端 DB 初始化阶段。
- `P0` 迁移后验证 Supabase Storage bucket。
  - 确认私有 bucket `route-media` 存在。
  - 确认已登录用户的云端路线可以上传打卡图、生成签名 URL 预览并删除图片。
- `P1` 推送/部署后，对 GitHub Pages 线上站点重新做一次真实冒烟测试。
  - 使用高德真实 POI 规划路线。
  - 至少验证南京和上海的沿途候选。
  - 保存云端路线、创建分享、撤销/删除分享。
  - 验证云端路线的途中打卡图上传。

## P0 产品流程缺口

- `P0` 让路线保存、分享、快照失败状态更可诊断。
  - 当前部分 Supabase/RLS/网络失败仍会被折叠成较泛的提示，例如云端操作暂时失败。
  - 需要为未登录、Supabase 未配置、路线 ID 迁移失败、RLS 拒绝、Storage bucket 缺失、网络超时等情况提供可执行提示。
- `P0` 确认站点角色和 provider 元数据在所有云端/读取路径中不丢失。
  - 本地路线站点已有 `routeRole`、开放时间、评分、人均和电话。
  - 需要验证云端路线、快照、分享路线、本地收藏在保存、重新读取、分享、复制/收藏、修改流程后仍保留这些字段。
- `P0` 明确收藏路线的持久化模型。
  - 当前收藏基于 localStorage。
  - 需要决定登录用户收藏是否同步到 Supabase。
  - 如需要云端同步，应新增 schema/RLS、repository 方法和迁移。
- `P0` 把“修改收藏/分享路线”升级成一等规划交接流程。
  - 当前修改会把路线载入本地预览并打开 `/plan/`。
  - 需要增加明确的“从收藏/分享导入”提示，并避免用户误覆盖原始分享或收藏路线。
- `P0` 完成路线结束后的行程存档。
  - 途中模式已按到达体验站点和上传照片结算分数。
  - 仍需要一个持久的行程存档视图，用于展示已完成路线、打卡图、分数、完成文案和完成日期。
- `P0` 验证所有编辑操作中的端点角色行为。
  - `必去` 或候选站点移动到首尾时，应继续保留深读、停留和打卡。
  - 显式 `出发`/`终点` 移到中间后，除非用户修改角色，否则仍应作为导航节点。
  - 增加角色变更和移动相关测试。

## P1 规划与候选质量

- `P1` 提升候选搜索质量，减少固定采样的局限。
  - 当前采样使用站点、中点和 provider polyline 点。
  - 长路线需要路线 polyline 密度控制。
  - 避免在密集城区过度查询，同时覆盖路线空白段。
- `P1` 提升餐厅推荐匹配度。
  - 当前餐食偏好会影响候选类型纳入和 AI/本地排序。
  - 需要更强地过滤菜系、人均、用餐时间、评分，以及午餐/晚餐时段是否营业。
  - 可参考大众点评式菜系分类，但要保持数据来源合法和技术上可控。
- `P1` 扩展开放时间解析。
  - 当前红色提示只解析简单 `HH:mm-HH:mm` 时间窗。
  - 需要支持多个时间段、跨夜、中文星期文本、“全天开放”、闭馆日、预约制说明和 provider 的模糊格式。
- `P1` 深化事实核验，不只依赖高德元数据。
  - 增加官方开放公告、门票/预约状态、闭馆提醒、来源引用和最近核验时间。
  - 不要把未核验 AI 内容当作事实来源展示。
- `P1` 为 DeepSeek schema 校验失败增加一次修复重试。
  - 当前 DeepSeek JSON schema 解析失败会直接 fallback。
  - 可把 schema 错误反馈给模型，最多修复一次，然后再 fallback。
- `P1` 执行 AI 用量限制。
  - `route_ai_runs` 日志已存在。
  - 需要基于 `AI_DAILY_USER_LIMIT` 和 `AI_PROJECT_COST_LIMIT_CNY` 实现每日用户限制和项目成本限制。
- `P1` 改进路线标题生成。
  - 当前标题是确定性生成。
  - 可增加 DeepSeek 路线标题生成，并保留本地 fallback 和最大长度限制。
- `P1` 增加路线冲突持久化。
  - 路线内核能检测缺失路段和固定时间冲突。
  - 需要把冲突结果或派生校验状态保存到编辑流程，方便用户回看未解决问题。

## P1 地图与路线 Provider

- `P1` 增强地图视野行为。
  - 路线编辑后更好地 fit bounds。
  - 高亮选中站点和选中路段。
  - 在地图上预览候选点插入位置。
- `P1` 增加移动端/全屏导航地图模式。
  - 当前移动端路线阅读页能保持地图和时间线关联，但还不是现场导航优先的体验。
- `P1` 决定是否需要 provider-backed 骑行。
  - 步行、公交、驾车、打车已可在 provider 可用时通过高德代理计算。
  - 骑行仍使用本地估算。
- `P1` 增加实时 provider 状态诊断。
  - 展示高德 JS、高德 Web Service、DeepSeek、Supabase Auth、Supabase DB 和 Storage 是否真实可达，而不仅是“已配置”。

## P1 认证、云端与数据可靠性

- `P1` 验证生产环境 Auth redirect 配置。
  - GitHub Pages URL：`https://999hkkzjvy-cell.github.io/culture-city-walk/`
  - 通配 Redirect：`https://999hkkzjvy-cell.github.io/culture-city-walk/**`
- `P1` 改进登录后的本地草稿迁移。
  - 当前同步是 best effort。
  - 需要更明确的“保存本地草稿到云端”流程，并处理云端已有路线时的冲突。
- `P1` 增加头像上传。
  - 当前 profile 只支持头像 URL。
  - 需要决定头像 Storage 的私有/公开策略，并增加图片上传 UI。
- `P1` 改进路线归档空态与错误态。
  - 区分“没有路线”、“未登录”、“云端不可用”和“主题筛选为空”。
- `P1` 增加 Supabase 路线/候选/快照完整性测试。
  - 本地 repository 和纯函数已有测试。
  - 需要补云端保存、路线重新读取、候选重新读取、快照创建/读取、分享创建/删除和 Storage 上传的集成检查。

## P2 UI 与内容打磨

- `P2` 提升 UI 参考图还原度。
  - 首页模式卡仍使用线性图标，而不是更丰富的物件图像。
  - 规划页移动端摘要还不是 mock 中的右侧纸卡形态。
- `P2` 改进规划页操作体验。
  - 小屏上路线摘要需要更易用，不应压迫对话流程。
  - 高德 POI、地点角色、餐食偏好和候选筛选需要更清晰的选中态。
- `P2` 改进路线阅读页排版和密集状态。
  - 长站点名、长开放时间字符串和 provider 警告需要更多移动端边界 QA。
- `P2` 增加更丰富的推荐路线内容。
  - 当前推荐路线浏览器仍是静态内容。
  - 可增加更多城市、更多标签、路线预览和适合分享的文案。
- `P2` 视需要增加分享海报导出。
  - 当前有意不做手工分享码输入或海报流程。
  - 等小红书/海报/口令分享用例明确后再评估。

## 下一次发布前操作清单

1. 运行本地验证：
   - `npx tsc --noEmit`
   - `npm run lint`
   - `npm test`
   - `npm run build`
   - `npm run e2e`
2. 应用 Supabase 迁移，尤其是 `route_checkin_photos`。
3. 部署变更过的 Edge Functions：
   - `amap-proxy`
   - 仅在 `deepseek-proxy` 代码变化时部署 `deepseek-proxy`
4. 使用中文提交信息提交。
5. 推送分支并确认 GitHub Pages workflow 成功。
6. 线上冒烟测试：路线规划、云端保存、分享、撤销/删除、途中打卡图归档、分享路线收藏/修改。

## 文档维护注意

- 以后 `docs/` 和项目内其他 Markdown 文档默认使用中文编写。
- `ignore-files/项目提交日志.md` 中较早的条目提到手工添加和只复核步行，这些是历史提交记录，不应当作当前行为说明。
